import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["images.unsplash.com", "www.pexels.com", "images.freepik.com"],
  },
};


module.exports = nextConfig;